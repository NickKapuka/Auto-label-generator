# Editable data

The files that are inside of this folder contain lookup tables, exclusion files, and text-replacement rules

These files may be updated without changing the label-processing logic itself and allows for easy updating

Keep existing variable names themselves unchanged as those are what the logic calls, but entries (denoted between '' and separated by commas) can be added or removed as desired.